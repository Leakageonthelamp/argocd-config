apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golang-http-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: golang-http-service
            port:
              number: 80